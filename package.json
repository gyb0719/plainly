{
  "name": "plainly",
  "version": "1.0.0",
  "private": true,
  "description": "DeepSeek 기반 웹 페이지 번역 Chrome Extension",
  "scripts": {
    "dev": "cd extension && npm run dev",
    "build:extension": "cd extension && npm run build",
    "deploy": "vercel"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  }
}
